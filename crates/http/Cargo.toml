[package]
name = "one-research-http"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
log = "0.4"
